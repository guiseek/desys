import { sharedUiFoundationStyles } from './shared-ui-foundation-styles';

describe('sharedUiFoundationStyles', () => {
  it('should work', () => {
    expect(sharedUiFoundationStyles()).toEqual('shared-ui-foundation-styles');
  });
});
