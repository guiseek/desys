import { sharedUiCustomElements } from './shared-ui-custom-elements';

describe('sharedUiCustomElements', () => {
  it('should work', () => {
    expect(sharedUiCustomElements()).toEqual('shared-ui-custom-elements');
  });
});
